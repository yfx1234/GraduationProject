# Legacy Compatibility

This directory is no longer the primary home for drone interception scripts.

Use `PythonClient/Drone/auto_spawn_visual_intercept.py` for active development.
The file kept here is only a compatibility wrapper so older commands and notes
still work.
