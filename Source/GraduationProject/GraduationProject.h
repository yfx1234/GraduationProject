// 解释：使用 `#pragma once` 防止该头文件在编译过程中被重复包含。
#pragma once

// 解释：引入 Unreal 的核心基础头文件，提供常用容器、数学类型和日志宏。
#include "CoreMinimal.h"

/**
 * @brief GraduationProject 模块公共头
 * 当前文件仅负责引入 Unreal 核心类型，并为后续模块级公共定义预留统一入口。
 */
